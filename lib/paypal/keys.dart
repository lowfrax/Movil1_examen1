// Este archivo ya no se usa directamente
// Las credenciales se obtienen desde Supabase usando PayPalKeyService
// Se mantiene por compatibilidad si es necesario

class PayPalKeys {
  // Este archivo está deprecado
  // Usar PayPalKeyService para obtener las credenciales desde Supabase
  // Modo sandbox se detecta automáticamente basado en el clientId
}
